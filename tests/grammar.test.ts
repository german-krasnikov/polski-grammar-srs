import {describe,it,expect} from 'vitest';import {nounForm,nounPhrase,verbForm} from '../src/grammar/engine';describe('grammar engine',()=>{it('handles masculine accusative animacy',()=>{expect(nounForm('husband','acc')).toBe('męża');expect(nounForm('dog','acc')).toBe('psa');expect(nounForm('house','acc')).toBe('dom')});it('handles feminine agreement',()=>{expect(nounPhrase('wife','acc',{adjectiveId:'nice',possessive:true})).toBe('moją ładną żonę');expect(nounPhrase('wife','gen',{adjectiveId:'nice',possessive:true})).toBe('mojej ładnej żony')});it('conjugates core tenses',()=>{expect(verbForm('see','present',1,'sg','m-personal')).toBe('widzę');expect(verbForm('see','past',1,'sg','m-personal')).toBe('widziałem');expect(verbForm('read','future',1,'sg','m-personal')).toBe('będę czytać');expect(verbForm('buyDone','future',1,'sg','m-personal')).toBe('kupię')})});